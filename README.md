# mist-drive
City of Vernonia ArcGIS JavaScript template application with Typescript, Sass and Webpack.

Bootstrap:

```shell
npm install
```

Development:

```shell
npm start
```

Build:

```shell
npm run build
```
